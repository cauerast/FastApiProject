Hello there, :)

create the virtual envoirment
    py -m venv .venv

instal the dependences:
    py -m pip install fastapi uvicorn sqlalchemy sqlalchemy_utils alembic bcrypt=4.0.1 passlib[bcrypt] python-jose[cryptography] python-dotenv python-multipart 

to run the project:
    py -m uvicorn main:app --reload

to change anything in db (use alembic) 
    (to do a migration) = changes
    alembic init alembic # create a folder with alembic dependences
    alembic revision --autogenerate -m "{message}" # create a new migration
    alembic upgrade head # apply the migration to the database